import React from "react";

function Preview() {
  return (
    <section
      className="relative z-20 bg-[var(--accent)] text-[var(--text)] font-inter py-6 selection:bg-[var(--accent)]"
      id="preview"
    >
      //Slideshow einfügen mit Anordnung von Bildern die Inhalte der App zeigen.
      <div></div>
    </section>
  );
}

export default Preview;
